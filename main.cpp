#include <iostream>

using namespace std;

int main()
{
    int a;
    int b;
    int c;

    cin >> a;
    cin >> b;
    cin >> c;

    if(a >= 0 || b >=0 || c >= 0 || a <= 1000 || b <= 1000 || c <= 1000)
    {
        
    }

    cout << "dziala" << endl;
    return 0;
}
